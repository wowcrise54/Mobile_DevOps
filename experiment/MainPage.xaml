<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            x:Class="experiment.MainPage"
            xmlns:local="clr-namespace:experiment"
            BarBackgroundColor="Black"
            SelectedTabColor="#A93240"
            UnselectedTabColor="White"
            xmlns:android="clr-namespace:Xamarin.Forms.PlatformConfiguration.AndroidSpecific;assembly=Xamarin.Forms.Core"
            android:TabbedPage.ToolbarPlacement="Bottom"
            android:TabbedPage.IsSwipePagingEnabled="False"
            android:TabbedPage.IsSmoothScrollEnabled="False">


    <NavigationPage Title="Главная" IconImageSource="main.png">
        <x:Arguments>
            <local:Home x:Name="Home" Title="Главная" />
        </x:Arguments>
    </NavigationPage>

    <NavigationPage Title="Избранное" IconImageSource="favourite.png">
        <x:Arguments>
            <local:like x:Name="like" Title="Избранное"/>
        </x:Arguments>
    </NavigationPage>

    <NavigationPage Title="Праздники" IconImageSource="holidays.png">
        <x:Arguments>
            <local:holidays x:Name="holidays" Title="Праздники"/>
        </x:Arguments>
    </NavigationPage>

    <NavigationPage Title="Настройки" IconImageSource="settings.png">
        <x:Arguments>
            <local:settings x:Name="settings" Title="Настройки"/>
        </x:Arguments>
    </NavigationPage>


</TabbedPage>